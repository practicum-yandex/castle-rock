import React from "react";

import { Component } from "@/utils/components";

import { CustomLayout } from "./Layout.styles";

const Layout: Component = (props) => <CustomLayout {...props} />;

export default Layout;
